## 1. 관련 이슈
- 관련 이슈 없음

## 2. 작업 모듈 (해당 항목에 x 표시)
- [ ] user-backend / user-frontend / user-fastapi
- [x] admin-backend / admin-frontend / admin-fastapi

## 3. 변경 사항 안내
- scraping 백엔드 Phase 1 범위로 `ScrapingPipeline`, `ScrapingLog` 엔티티와 상태/액션 Enum을 추가했습니다.
- scraping 백엔드 Phase 2 범위로 `ScrapingPipelineRepository`, `ScrapingLogRepository`를 추가하고 `sourceName` 기준 단건 참조 메서드를 정의했습니다.
- Repository가 직접 집계/상태 전이/실행 로그 생성 주체가 아님을 주석으로 명시해 Spring/FastAPI 책임 경계를 드러냈습니다.
- `tasks.md`, `checklist.md`를 현재 구현 단계에 맞게 정리하고 Phase 1, 2 완료 상태를 반영했습니다.

## 4. 테스트 및 검증 결과
- [ ] 로컬 환경 컴파일 및 빌드 성공 여부
- [ ] 비즈니스 로직 단위 테스트(Unit Test) 통과 여부
- [ ] API 기능 작동 확인 (Postman / Swagger 등 수동 테스트 결과)

- 이번 PR에서는 Phase 1, 2 범위 구현과 문서 정합성 정리까지 반영했습니다.

## 5. 리뷰어 전달 사항
- 아직 Service/FastAPI Integration/API 계층은 구현 전입니다. 이번 PR은 Entity/Repository 범위만 포함합니다.
- `checklist.md`는 Phase 기준으로 재구성해 이후 구현 진행 중 점검하기 쉽게 정리했습니다.