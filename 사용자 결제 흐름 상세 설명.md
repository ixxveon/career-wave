# 사용자 결제 흐름 상세 설명 / 用户支付完整流程说明

> Phase 4 — Toss Payments billingKey 자동결제 구독 시스템
> 최종 업데이트: 2026-06-23

---

# 한국어 버전

## 1단계 — 상품 선택 (FE)

사용자가 구독 페이지에서 상품(면접 준비 / 서류 코칭)을 선택하고 "자동 정기 결제 및 이용 조건에 동의" 체크박스에 체크 후 결제 버튼을 클릭합니다.

---

## 2단계 — 주문 생성 (`POST /api/v1/user/billing/checkout/orders`)

**FE → BE**: `CreateOrderRequest { productCode, successUrl, failUrl }`

**BE 처리 (`UserCheckoutOrderServiceImpl.createOrder`)**:
1. JWT에서 `memberId` 추출
2. `isEligibleForBilling()` → 계정 상태 ACTIVE 여부 확인 (정지·탈퇴 계정 차단)
3. Plan 존재 여부 + `isActive` 확인
4. 이미 ACTIVE / CANCEL_SCHEDULED / PAYMENT_FAILED 구독이 있으면 409 반환
5. 동일 `memberId + planId`로 READY 주문이 이미 있으면 **그 주문 그대로 반환** (멱등성)
6. 신규 주문 생성:
   - `orderId = "ORDER-" + UUID` (32자 hex)
   - `idempotencyKey = UUID` (Toss API 중복 요청 방지용)
   - `customerKey = UUID` (이 회원의 영구 식별자, Toss billingKey와 연결)
   - `expiresAt = now + 30분`
7. `UserPayment` (status=READY) + `BillingConsent` 엔티티 DB 저장
8. `CreateOrderResponse` 반환 (orderId, customerKey, amount=29000 등)

---

## 3단계 — Toss SDK 결제창 호출 (FE)

`useCheckoutStatus.ts`가 아래 흐름으로 결제창을 초기화합니다.

```js
const payment = tossPayments.payment({ customerKey: order.customerKey });
await payment.requestBillingAuth({ method: 'CARD', successUrl, failUrl, ... });
```

→ **브라우저가 Toss 카드 등록 UI로 이동**

---

## 4단계 — 사용자가 카드 정보 입력 (Toss UI)

Toss가 자체 UI에서 카드사 선택, 카드번호, CVC, 생년월일 등을 수집합니다.  
**서비스 서버는 완전한 카드번호를 절대 수신하지 않습니다.** Toss는 마스킹된 값만 반환합니다.

---

## 5단계 — Toss Redirect 콜백

| 결과 | 이동 URL |
|------|---------|
| 성공 | `successUrl?authKey=xxx&customerKey=UUID&orderId=ORDER-xxx` |
| 실패 | `failUrl?orderId=ORDER-xxx&code=xxx&message=xxx` |

---

## 6단계 — 결제 확인 (`POST /api/v1/user/billing/payments/confirm`)

**FE → BE**: `ConfirmPaymentRequest { authKey, customerKey, orderId }`  
(`usePaymentSuccessStatus.ts`가 URL params 파싱 후 자동 호출)

**BE 처리 (`UserPaymentConfirmServiceImpl.confirm`)** — **@Transactional 없음** (Toss 외부 API 호출이 트랜잭션 안에 있으면 안 됨):

### 6-1. 주문 검증
- orderId로 `UserPayment` 조회 (없으면 404)
- status == READY 확인 (이미 처리된 주문이면 409)
- memberId 일치 확인 (타인 주문 접근 차단)
- customerKey 일치 확인 (위변조 방지)
- 주문 만료(expiresAt) 확인

### 6-2. Toss billingKey 발행 (`TossBillingAuthorizationClient`)

```
POST https://api.tosspayments.com/v1/billing/authorizations/issue
Authorization: Basic Base64(TOSS_SECRET_KEY + ":")
Body: { authKey, customerKey }
```

→ 응답: `{ billingKey, customerKey, authenticatedAt, card: { company, number(마스킹) } }`

실패 시: `failureTxService.failPayment()` — **REQUIRES_NEW 별도 트랜잭션**으로 READY→FAILED 저장  
(메인 트랜잭션이 없어도 독립 커밋으로 실패 기록이 반드시 보존됨)

### 6-3. BillingProfile 저장 (카드 정보)

- billingKey를 **AES-256/GCM으로 암호화** (IV 12바이트 랜덤 → Base64 저장)
- Toss가 반환한 마스킹 카드번호만 저장, **완전한 카드번호 저장 절대 금지**
- `BillingProfile` 엔티티 저장 → `billingProfileId` 확보
- **포인트**: 실제 결제 전에 미리 저장 → 결제 실패해도 카드 등록 정보는 보존됨

### 6-4. 실제 결제 실행 (`TossBillingPaymentClient`)

```
POST https://api.tosspayments.com/v1/billing/{billingKey}
Authorization: Basic Base64(TOSS_SECRET_KEY + ":")
Body: {
  customerKey,
  amount: plan.planPrice,   ← DB에서 가져온 금액 (클라이언트 금액 절대 신뢰 안 함)
  orderId,
  orderName: plan.productName,
  customerName,
  customerEmail
}
```

→ 응답: `{ paymentKey, orderId, status: "DONE", totalAmount, approvedAt }`

실패 시: `failureTxService.failPayment()` → AUTHORIZED→FAILED

### 6-5. 금액 검증

`response.totalAmount != plan.planPrice` → 예외 발생 (금액 조작 방지)

### 6-6. 원자 정산 (`settle()` — **@Transactional 단일 트랜잭션**)

아래 6가지 DB 상태 변경이 **하나의 트랜잭션**으로 커밋됩니다. 일부만 성공하는 경우는 없습니다.

| 순서 | 작업 | 상태 변경 |
|------|------|---------|
| 1 | `payment.authorize()` | READY → AUTHORIZED |
| 2 | `payment.confirmStarted()` | AUTHORIZED → CONFIRMING |
| 3 | `payment.paid(approvedAt, paymentKey)` | CONFIRMING → PAID |
| 4 | `Subscription.create(memberId, planId, billingProfileId, ...)` | 신규 ACTIVE 구독 생성 |
| 5 | `entitlement.forfeitFree()` → `activatePremium(subscriptionId)` | FREE → PREMIUM 권익 전환 |
| 6 | `SubscriptionUsagePeriod.create(...)` | 사용량 주기 생성 (면접 20회/월, 서류 30회/월) |

`nextBillingAt = approvedAt + 30일 (고정)`

**FE 응답**: `ConfirmPaymentResponse { paymentStatus: "PAID", subscriptionStatus: "ACTIVE", paidAt, nextBillingAt, ... }`

---

## 7단계 — 결제 완료 화면 (FE)

`usePaymentSuccessStatus.ts`가 confirm 응답 수신 후 `navigate(pathname, { replace: true })`로  
URL의 authKey/customerKey를 브라우저 히스토리에서 제거하고, 결제 완료 UI를 표시합니다.

---

## 실패 흐름

**Toss redirect fail URL 도착 시**:
- FE가 `POST /api/v1/user/billing/payments/fail` 호출
- `RecordPaymentFailRequest { orderId, reasonCode, message }`
- BE: `failureTxService.failPayment()` → READY→FAILED 기록
- 응답의 `retryable: true` → FE가 재시도 버튼 표시

| 실패 사유 | 재시도 가능 여부 |
|---------|--------------|
| USER_CANCELED (사용자 직접 취소) | ✅ 가능 |
| CARD_DECLINED (카드 승인 거절) | ✅ 가능 |
| TIMEOUT (시간 초과) | ✅ 가능 |
| DUPLICATE_ORDER (중복 주문) | ❌ 불가 |
| CONFIRM_FAILED (승인 실패) | ❌ 불가 |
| FORBIDDEN (계정 제한) | ❌ 불가 |
| UNKNOWN (알 수 없는 오류) | ❌ 불가 |

---

## 보조 기능

### 주문 상태 조회 (`GET /api/v1/user/billing/payments/orders/{orderId}`)
- FE polling 용도 (CONFIRMING 중 새로고침 시)
- 내부 상태 AUTHORIZED / RECONCILING는 FE에 **CONFIRMING**으로 노출

### 만료 스케줄러 (`OrderExpirationScheduler`)
- **5분마다** 자동 실행
- `expiresAt <= now`인 READY 주문 → CANCELED 일괄 처리
- 30분 방치된 미결제 주문 자동 정리

---

## 보안 요약

| 항목 | 처리 방식 |
|------|---------|
| billingKey | AES-256/GCM 암호화 저장, API 응답·로그·예외에 절대 미포함 |
| 카드번호 | Toss 마스킹 값만 저장, 완전한 카드번호 저장 금지 |
| 결제 금액 | 서버 DB(plan.planPrice)에서 결정, 클라이언트 전달값 무시 |
| Toss 시크릿키 | 환경변수 `TOSS_SECRET_KEY`로만 주입, 하드코딩 금지 |
| 암호화 키 | 환경변수 `TOSS_BILLING_KEY_ENCRYPTION_KEY`로만 주입 |

---
---

# 中文版本

## 第一步 — 选择商品（前端）

用户在订阅页面选择商品（面试准备 / 文书辅导），勾选"同意自动定期扣款及使用条款"复选框，点击支付按钮。

---

## 第二步 — 创建订单（`POST /api/v1/user/billing/checkout/orders`）

**前端 → 后端**：`CreateOrderRequest { productCode, successUrl, failUrl }`

**后端处理（`UserCheckoutOrderServiceImpl.createOrder`）**：
1. 从 JWT 提取 `memberId`
2. `isEligibleForBilling()` → 验证账户状态为 ACTIVE（封禁/注销账户直接拒绝）
3. 验证商品（Plan）存在且 `isActive = true`
4. 若已有 ACTIVE / CANCEL_SCHEDULED / PAYMENT_FAILED 状态的订阅 → 返回 409
5. 若同一 `memberId + planId` 已有 READY 状态的订单 → **直接返回该订单**（幂等性保障，不重复创建）
6. 创建新订单：
   - `orderId = "ORDER-" + UUID`（32位 hex 字符串）
   - `idempotencyKey = UUID`（防止 Toss API 重复请求）
   - `customerKey = UUID`（该会员的永久标识符，与 billingKey 绑定）
   - `expiresAt = 当前时间 + 30分钟`
7. 保存 `UserPayment`（status=READY）+ `BillingConsent` 实体到 DB
8. 返回 `CreateOrderResponse`（包含 orderId、customerKey、amount=29000 等）

---

## 第三步 — 调起 Toss SDK 支付界面（前端）

`useCheckoutStatus.ts` 执行：

```js
const payment = tossPayments.payment({ customerKey: order.customerKey });
await payment.requestBillingAuth({ method: 'CARD', successUrl, failUrl, ... });
```

→ **浏览器跳转至 Toss 卡片登记 UI**

---

## 第四步 — 用户输入卡片信息（Toss UI 内）

Toss 自己的界面收集卡号、有效期、CVC 等信息。  
**我方服务器始终不接触完整卡号**，Toss 仅返回脱敏后的卡号（如 `****-****-****-1234`）。

---

## 第五步 — Toss Redirect 回调

| 结果 | 跳转 URL |
|------|---------|
| 成功 | `successUrl?authKey=xxx&customerKey=UUID&orderId=ORDER-xxx` |
| 失败 | `failUrl?orderId=ORDER-xxx&code=xxx&message=xxx` |

---

## 第六步 — 确认支付（`POST /api/v1/user/billing/payments/confirm`）

**前端 → 后端**：`ConfirmPaymentRequest { authKey, customerKey, orderId }`  
（`usePaymentSuccessStatus.ts` 自动解析 URL 参数并调用）

**后端处理（`UserPaymentConfirmServiceImpl.confirm`）** — **此方法不加 @Transactional**（Toss 外部 API 调用不能在事务内）：

### 6-1. 订单验证
- 用 orderId 查询 `UserPayment`（不存在 → 404）
- 验证 status == READY（已处理过 → 409）
- 验证 memberId 归属（防止操作他人订单）
- 验证 customerKey 匹配（防篡改）
- 验证订单未过期（expiresAt 检查）

### 6-2. 向 Toss 申请 billingKey（`TossBillingAuthorizationClient`）

```
POST https://api.tosspayments.com/v1/billing/authorizations/issue
Authorization: Basic Base64(TOSS_SECRET_KEY + ":")
Body: { authKey, customerKey }
```

→ 响应：`{ billingKey, customerKey, authenticatedAt, card: { company, number(脱敏) } }`

**若失败**：调用 `failureTxService.failPayment()` — 使用 **REQUIRES_NEW 独立事务**将订单状态改为 FAILED 并单独提交  
（即使主流程没有事务，失败记录也能独立持久化）

### 6-3. 保存 BillingProfile（卡片信息）

- 用 **AES-256/GCM** 加密 billingKey（随机 12 字节 IV → Base64 存储）
- 仅保存 Toss 返回的脱敏卡号，**完整卡号永不存储**
- 保存 `BillingProfile` 实体 → 获取 `billingProfileId`
- **关键点**：在实际扣款前就先保存 → 即使后续扣款失败，卡片登记信息依然保留

### 6-4. 执行实际扣款（`TossBillingPaymentClient`）

```
POST https://api.tosspayments.com/v1/billing/{billingKey}
Authorization: Basic Base64(TOSS_SECRET_KEY + ":")
Body: {
  customerKey,
  amount: plan.planPrice,   ← 金额来自服务器 DB，绝不信任客户端传来的金额
  orderId,
  orderName: plan.productName,
  customerName,
  customerEmail
}
```

→ 响应：`{ paymentKey, orderId, status: "DONE", totalAmount, approvedAt }`

**若失败**：`failureTxService.failPayment()` → AUTHORIZED→FAILED

### 6-5. 金额验证

`response.totalAmount != plan.planPrice` → 抛出异常（防止金额被篡改）

### 6-6. 原子结算（`settle()` — **@Transactional 单一事务**）

以下 6 个 DB 状态变更在**同一个事务**中完成，要么全部成功，要么全部回滚：

| 顺序 | 操作 | 状态变更 |
|------|------|---------|
| 1 | `payment.authorize()` | READY → AUTHORIZED |
| 2 | `payment.confirmStarted()` | AUTHORIZED → CONFIRMING |
| 3 | `payment.paid(approvedAt, paymentKey)` | CONFIRMING → PAID |
| 4 | `Subscription.create(memberId, planId, billingProfileId, ...)` | 新建 ACTIVE 订阅 |
| 5 | `entitlement.forfeitFree()` → `activatePremium(subscriptionId)` | FREE → PREMIUM 权益变更 |
| 6 | `SubscriptionUsagePeriod.create(...)` | 新建使用量周期（面试 20次/月，文书 30次/月） |

`nextBillingAt = approvedAt + 30天（固定）`

**返回给前端**：`ConfirmPaymentResponse { paymentStatus: "PAID", subscriptionStatus: "ACTIVE", paidAt, nextBillingAt, ... }`

---

## 第七步 — 显示支付完成页面（前端）

`usePaymentSuccessStatus.ts` 收到 confirm 响应后，通过 `navigate(pathname, { replace: true })` 将 URL 中的 authKey/customerKey 从浏览器历史中清除，展示支付成功 UI。

---

## 失败流程

**Toss 跳转至 failUrl 时**：
- 前端调用 `POST /api/v1/user/billing/payments/fail`
- 后端记录失败原因，READY → FAILED
- 响应中 `retryable: true` 时前端显示"重试"按钮

| 失败原因 | 是否可重试 |
|---------|-----------|
| USER_CANCELED（用户主动取消） | ✅ 可重试 |
| CARD_DECLINED（卡片被拒） | ✅ 可重试 |
| TIMEOUT（超时） | ✅ 可重试 |
| DUPLICATE_ORDER（重复订单） | ❌ 不可重试 |
| CONFIRM_FAILED（确认失败） | ❌ 不可重试 |
| FORBIDDEN（账户受限） | ❌ 不可重试 |
| UNKNOWN（未知错误） | ❌ 不可重试 |

---

## 辅助功能

### 订单状态查询（`GET /api/v1/user/billing/payments/orders/{orderId}`）
- 供前端轮询使用（CONFIRMING 状态下刷新页面时）
- 内部状态 AUTHORIZED / RECONCILING 对前端统一显示为 **CONFIRMING**

### 订单过期定时任务（`OrderExpirationScheduler`）
- **每5分钟**自动执行一次
- 查找 `expiresAt <= 当前时间` 且 status=READY 的订单 → 批量改为 CANCELED
- 自动清理30分钟内未完成支付的僵尸订单

---

## 安全要点总结

| 项目 | 处理方式 |
|------|---------|
| billingKey | AES-256/GCM 加密存储，不出现在日志/异常/API 响应中 |
| 卡号 | 仅存储 Toss 返回的脱敏值，完整卡号永不存储 |
| 支付金额 | 由服务器 DB（`plan.planPrice`）决定，客户端传值无效 |
| Toss 密钥 | 通过环境变量 `TOSS_SECRET_KEY` 注入，严禁硬编码 |
| 加密密钥 | 通过环境变量 `TOSS_BILLING_KEY_ENCRYPTION_KEY` 注入 |
