# Checklist: Document Analysis (서류 코칭)

> 작성자: 이가연 | 작성일: 2026-05-29  
> 구현 완료 후 PR 올리기 전에 본인이 직접 체크합니다.  
> PR 제목 형식: `[RESUME] 서류 분석 기능 구현`

---

## Phase 1 — 상태 관리 & 데이터 연동

### 상태 설계 및 성능 최적화
- [ ] `Zustand` 스토어: 서버 상태(서류 분석 결과)와 UI 로컬 상태(modal open, input value)가 분리되어 있는가?
- [ ] 데이터 무결성: 전역 상태를 로컬 `useState`에 복사하여 의존성이 꼬이게 만들지 않았는가?
- [ ] 리렌더링 최적화: `React.memo`, `useMemo`, `useCallback`을 사용하여 불필요한 리렌더링을 방지했는가?
- [ ] 상태 전이: `idle → uploading → analyzing → success / error` UI 상태 전이가 누락 없이 처리되는가?

### 데이터 연동
- [ ] 데이터 계약: `api-schema.md`에 정의된 JSON 구조와 실제 API 호출/응답 구조가 100% 일치하는가?
- [ ] 세션 유지: 페이지 새로고침 시에도 `documentId` 및 결과 데이터가 유실되지 않는가? (SessionStorage 적용)
- [ ] 분석 중 페이지 이탈 후 재진입 시, 진행 중이던 상태가 올바르게 복원되는가?

### 에러 핸들링
- [ ] API 호출 실패 시 `ErrorCode`에 따른 공통 모달/메시지가 정상 노출되는가?
- [ ] 로딩 타임아웃: API 응답이 없을 때 적절한 타임아웃 처리 후 에러 모달이 노출되는가?
- [ ] 네트워크 단절 상태에서 업로드 시 적절한 에러 메시지가 표시되는가?

---

## Phase 2 — 기능 구현 (P1: 핵심 사용자 플로우)

### 파일 업로드
- [ ] 용량 제한: 10MB 초과 파일 업로드 시 즉시 차단 및 경고 메시지가 노출되는가?
- [ ] 확장자 검증: PDF, DOC, DOCX 이외의 파일 업로드 시 차단되는가?
- [ ] MIME 타입 검증: 클라이언트 측에서 실제 파일 형식을 검증하는가?
- [ ] 중복 요청 방지: 업로드 버튼이 클릭되는 순간 `disabled` 처리되어 중복 요청을 막는가?

### 분석 로딩 (Modal)
- [ ] 로딩 상태 동기화: 분석 단계(파일 파싱 → AI 분석 → 리포트 생성)에 따라 메시지가 동적으로 변경되는가?
- [ ] 비정상 종료 대응: 분석 중 API 에러 발생 시 로딩 모달이 닫히고 에러 알림이 뜨는가?

### 리포트 시각화
- [ ] 종합 점수 애니메이션: 0에서 N점까지 차오르는 부드러운 수치 애니메이션이 적용되었는가?
- [ ] 리포트 구조: Good / Bad / Fix 3단계 피드백이 누락 없이 UI에 매핑되었는가?

---

## Phase 3 — 연동 계약 (P2: 타 도메인 연결)

### 면접 도메인 연결
- [ ] 면접 진입: "이 서류로 면접 시작하기" 클릭 시 `documentId`가 면접 페이지로 정상 전달되는가?
- [ ] 데이터 검증: 전달된 `documentId`가 유효하지 않을 경우, 면접 페이지에서 에러 처리(404 모달 등)가 되는가?

### 목록 관리
- [ ] 히스토리 조회: 서류 분석 이력이 최신순으로 페이징 처리되어 정상 조회되는가?
- [ ] 상태 표시: 리스트 내에서 '진행 중', '완료', '실패' 상태가 즉각적으로 반영되는가?

---

## 보안 및 접근성

- [ ] IDOR 방어: 본인 소유가 아닌 `documentId` 접근 시 API 단에서 403 차단이 이루어지는가?
- [ ] 보안 준수: 브라우저 전역 객체(`window`)에 세션 정보나 민감 데이터를 저장하지 않았는가?
- [ ] a11y(접근성): 파일 업로드/결과 화면이 키보드만으로 플로우 완주가 가능한가? (aria-label 설정 완료)

---

## 코드 품질 & 컨벤션

- [ ] **레이어 분리**: API 로직(`services/`)과 UI 컴포넌트(`components/`)가 명확히 분리되었는가?
- [ ] **재사용성**: 공통 Button, Modal, LoadingSpinner를 사용했는가?
- [ ] **테스트**: 파일 검증 및 피드백 데이터 매핑 로직에 대한 단위 테스트가 작성되었는가?

---

## 배포 전 최종 확인

- [ ] **클린업**: 하드코딩된 목업 데이터(MSW 외) 및 `console.log`가 제거되었는가?
- [ ] **품질**: Lighthouse 성능 90점 이상, 접근성 95점 이상 확인.
- [ ] **마감**: `tasks.md` 항목들이 모두 완료 체크되었는가?