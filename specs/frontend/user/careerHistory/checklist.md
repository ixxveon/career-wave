# Checklist: Career History

## Phase 1 — 도메인 구조 설계

- [x] Career History 도메인의 핵심 기능 범위가 명확히 정의되었는가?
- [x] 취업 준비 기록 및 히스토리에서 관리할 데이터 항목이 정리되었는가?
- [ ] 면접 연습 기록에 필요한 텍스트/비디오 모의면접 데이터 구조가 정의되었는가?
- [x] 누적 데이터 기반 AI 역량 평가에 필요한 점수 항목이 정의되었는가?
- [x] 맞춤형 로드맵 가이드에 필요한 약점 분석 및 추천 액션 플랜 구조가 정의되었는가?
- [x] Document Analysis, Interview 도메인과의 연관 데이터가 확인되었는가?

## Phase 2 — 엔티티 & 레포지토리

- [ ] `CareerHistory` 엔티티 필드 및 제약조건이 적용되었는가?
- [ ] `InterviewPracticeHistory` 엔티티 필드 및 제약조건이 적용되었는가?
- [ ] `CareerCompetencyReport` 엔티티 필드 및 제약조건이 적용되었는가?
- [ ] `CareerRoadmap` 엔티티 필드 및 제약조건이 적용되었는가?
- [ ] 필요한 enum 타입이 정의되었는가?
- [ ] `ActivityType` enum이 정의되었는가?
- [ ] `InterviewType` enum이 정의되었는가?
- [ ] `CareerHistoryStatus` enum이 정의되었는가?
- [ ] 필요한 Repository 인터페이스가 작성되었는가?
- [ ] 날짜별 조회에 필요한 Repository 쿼리 메서드가 구현되었는가?
- [ ] 기업별 조회에 필요한 Repository 쿼리 메서드가 구현되었는가?
- [ ] 사용자 ID 기준 조회 메서드가 구현되었는가?

## Phase 3 — 핵심 API

- [ ] 취업 준비 기록 목록 조회 엔드포인트가 구현되었는가?
- [ ] 취업 준비 기록 상세 조회 엔드포인트가 구현되었는가?
- [x] 날짜별 취업 준비 기록 조회 기능이 프론트 query parameter 기준으로 반영되었는가?
- [x] 기업별 취업 준비 기록 조회 기능이 프론트 query parameter 기준으로 반영되었는가?
- [ ] 면접 연습 기록 조회 엔드포인트가 구현되었는가?
- [ ] 질문-답변 매칭 데이터가 응답에 포함되는가?
- [ ] AI 오답 하이라이팅 및 개선 피드백 데이터가 응답에 포함되는가?
- [ ] 정상 케이스 응답에서 `ApiResponse<T>` 래퍼를 사용하는가?
- [ ] 예외 케이스가 `BusinessException(ErrorCode.XXX)` 패턴으로 처리되는가?
- [ ] 존재하지 않는 기록 조회 시 적절한 `ErrorCode`가 매핑되는가?

## Phase 4 — 부가 API & 프론트엔드 화면

- [x] 누적 서류 점수 및 면접 점수 기반 역량 평가 조회 기능이 구현되었는가?
- [x] 약점 분석 및 우선 학습 타겟 조회 기능이 구현되었는가?
- [x] 맞춤형 로드맵 조회 기능이 구현되었는가?
- [x] `CareerHistoryPage.tsx`에서 취업 준비 기록 목록이 렌더링되는가?
- [x] 날짜별/기업별 기록 목록 UI가 구현되었는가?
- [x] 기록 카드에 활동 유형, 기업명, 날짜, 점수, 상태가 표시되는가?
- [x] `CareerHistoryDetailPage.tsx`에서 상세 기록 화면이 렌더링되는가?
- [x] 면접 스크립트, 질문-답변 매칭 데이터, AI 피드백 영역이 표시되는가?
- [x] AI 오답 및 개선 필요 답변 하이라이팅 UI가 표시되는가?
- [x] `CareerCompetencyReportPage.tsx`에서 누적 역량 평가 화면이 렌더링되는가?
- [x] 서류 점수, 면접 점수, 종합 점수가 표시되는가?
- [x] 점수 변화 그래프 또는 통계 영역이 표시되는가?
- [x] `CareerRoadmapPage.tsx`에서 맞춤형 로드맵 화면이 렌더링되는가?
- [x] mock data 기준으로 화면이 깨지지 않고 출력되는가?

## Phase 5 — 문서화 & 테스트

- [ ] `CareerHistoryDocs` 인터페이스가 작성되었는가?
- [ ] Controller에 Swagger 어노테이션을 직접 작성하지 않았는가?
- [ ] 취업 준비 기록 목록 조회 API 문서가 작성되었는가?
- [ ] 취업 준비 기록 상세 조회 API 문서가 작성되었는가?
- [ ] 면접 연습 기록 조회 API 문서가 작성되었는가?
- [ ] 역량 평가 및 로드맵 조회 API 문서가 작성되었는가?
- [ ] 정상 케이스 통합 테스트가 작성되었는가?
- [x] 빈 데이터 케이스 대응이 프론트에서 처리되었는가?
- [ ] 일부 누락 데이터 케이스 테스트가 작성되었는가?
- [ ] 존재하지 않는 상세 기록 접근 케이스 테스트가 작성되었는가?

## 코드 품질

- [ ] `ApiResponse<T>` 래퍼 누락 엔드포인트가 없는가?
- [ ] `BusinessException(ErrorCode.XXX)` 패턴을 준수했는가?
- [ ] 컨트롤러에서 직접 `ResponseEntity`를 반환하지 않는가?
- [ ] 비즈니스 로직이 서비스 레이어에만 존재하는가?
- [ ] 엔티티가 컨트롤러 응답으로 직접 노출되지 않는가?
- [x] Request/Response DTO 또는 프론트 타입이 분리되어 있는가?
- [ ] `global/`에서 `domain/`을 참조하지 않는가?
- [x] 기존 사용자 페이지와 관리자 페이지에 영향을 주지 않는가?
- [x] mock data는 명시적으로 켠 경우에만 동작하도록 제한했는가?
- [x] 운영 기본값이 실제 API 호출로 고정되었는가?
- [x] 빈 query parameter가 실제 API 요청에 포함되지 않는가?
- [x] 목록 API 응답이 `null`일 경우 빈 배열로 방어 처리되는가?

## 머지 전 최종 확인

- [ ] `contracts/api-spec.json`이 실제 구현과 일치하는가?
- [ ] `constitution.md` 불변 규칙과 실제 구현이 일치하는가?
- [x] PR 제목 형식을 준수했는가?
- [ ] tasks.md 모든 항목이 완료 체크되었는가?
- [x] 브라우저 콘솔에 React 렌더링 오류 또는 import 오류가 없는가?
- [x] `npm run dev` 실행 시 사용자 페이지가 정상 렌더링되는가?
- [x] `npm run build`가 성공하는가?
- [x] GitHub CI 체크가 통과했는가?